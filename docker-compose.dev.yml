version: "3.8"
services:
  wg-easy:
    image: wg-easy
    command: npm run serve
    volumes:
      - ./src/:/app/
    ports:
      - "51821:51821/tcp"
    environment: 
      # - PASSWORD=p
      - WG_HOST=192.168.1.39
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
